## 1. 排序核心：歌手块提取与重排（纯函数）

- [x] 1.1 在 `src/sort.js` 新增 `extractArtistBlocks(tracks)`：线性扫描输出块序列 `[{ artistKey, displayName, tracks }]`（连续同 artistKey 的段为一个块，`__unknown__` 显示名"(无歌手信息)"）；用固定 tracks 构造 3 个用例验证（多歌手多块、无专辑歌块、单块）通过
- [x] 1.2 在 `src/sort.js` 新增 `reorderByArtistBlocks(tracks, artistOrder)`：按目标歌手 key 顺序拼接块，未列出的块按原相对顺序追加末尾；用例验证：完整顺序重排、部分顺序（容错不丢歌）、空顺序（原样返回）通过
- [x] 1.3 验证块完整性：重排前后每个块内的曲目序列逐位一致（用 1.1 的输出对比）

## 2. 交互向导：调整界面

- [x] 2.1 确认环节 select 新增"🎚️ 调整歌手顺序"选项；选中后用 `extractArtistBlocks(newTracks)` 进入调整界面，展示歌手列表（label: 显示名 + 歌曲数）
- [x] 2.2 实现调整循环：选歌手 → 选操作（上移/下移/置顶/置底/完成/放弃）→ 更新块顺序回到歌手选择；边界操作（首位上移/末位下移）不报错不变序
- [x] 2.3 完成路径：`reorderByArtistBlocks` 重排 newTracks → 回确认环节刷新预览 note 与位置变动；放弃路径：恢复进入前快照回确认环节；取消路径走既有 bail（exit 0 无请求）
- [x] 2.4 真实向导走一遍：选歌单 → 计算 → 调整两位歌手（一置顶一上移）→ 完成后预览首位变为置顶歌手 → 取消退出，验证云端无变化

## 3. 提交与验收

- [x] 3.1 真实提交一次带调整的排序：确认备份文件记录的是拉取时原序（非调整后顺序），提交成功后用回滚入口恢复，歌单回到操作前状态
- [x] 3.2 对照 `specs/artist-order-adjustment/spec.md` 逐条 Scenario 核验（进入/完成/放弃、四种移动、边界、块完整、不持久化）通过；`cli.js sort --dry-run` 回归确认命令行路径无变化
